@{
    Layout = null;
    ViewBag.Title = "Listado de ganadores";
}
<div class="col-lg-12 no-padder" ng-controller="WinnerListController" ng-init="app.title = '@(ViewBag.Title)'">
    <div class="col-lg-12 no-padder">
        <div class="panel panel-default">
            <div class="panel-body">
                <!-- START panel-->
                <div class="col-lg-12 text-right m-b-xs no-padder">
                    <a href="#/winner/winner/0" class="btn btn-square btn-success {{moduleCanAdd}}"><i class="fa fa-plus-square"></i> Agregar ganador</a>
                </div>
                <div class="row-border hover">
                    <table class="table dataTableGrid">
                        <thead>
                            <tr>
                                <th>Nombre</th>
                                <th>Tipo documento</th>
                                <th>Documento</th>
                                <th>Teléfono</th>
                                <th>Género</th>
                                <th class="editColumm">Acción</th>
                            </tr>
                        </thead>
                        <tbody>
                            <tr data-ng-repeat="winner in winnersList">
                                <td>{{winner.WinnerName}}</td>
                                <td>{{winner.DocumentType}}</td>
                                <td>{{winner.DocumentNumber}}</td>
                                <td>{{winner.Phone}}</td>
                                <td>{{winner.Gender}}</td>
                                <td>
                                    <a href="#/winner/winner/{{winner.Id}}" title="Modificar ganador" class="{{moduleCanEdit}}"><i class="fa fa-pencil-square-o"></i></a>
                                    <a ng-show="verifyCanView('app.AwardsHistory')" href="#/winner/awardsHistory/{{winner.Id}}" title="Lista de premios recibidos" class="{{moduleCanView}}"><i class="fa fa-trophy"></i></a>
                                </td>
                            </tr>
                        </tbody>
                    </table>
                </div>
                <!-- END table-responsive-->
            </div>
        </div>
    </div>
</div>
<div class="col-lg-12 no-padder text-left m-b-lg">
    <a ui-sref="app.dashboard" class="mr btn btn-square btn-default"><i class="fa fa-arrow-left "></i> Salir</a>
</div>
